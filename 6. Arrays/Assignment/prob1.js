// Given variables
let n = 5;  // example size of the array
let arr = [1, 2, 3, 4, 5];  // example array

// Traverse the array and print each element on a new line
for (let i = 0; i < n; i++) {
    console.log(arr[i]);
}



let arrd = [1,2,3,4,45,"eee"]
arrd.push("simd")
