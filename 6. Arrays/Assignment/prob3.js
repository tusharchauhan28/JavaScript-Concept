// Given input
let n = 5;  // example size of the array
let arr = [1, 2, 3, 4, 5];  // example array

// Reverse the array
let reversedArr = arr.reverse();

// Convert the reversed array to a string with space-separated values
let output = reversedArr.join(' ');

// Print the output
console.log(output);
