let arr = [1, 2, 3, 4, 5];
let n = 5;

for (let i = 0; i < n; i++) {
    if (arr[i] % 2 === 0) {  // check if the element is even
        console.log(arr[i]);
    }
}




// REVERSE ARRAY 
//-------------------------------------------------------------

// arr.reverse();

// // Traverse the reversed array and print only the even elements
// for (let i = 0; i < n; i++) {
//     if (arr[i] % 2 === 0) {  // check if the element is even
//         console.log(arr[i]);
//     }
// }